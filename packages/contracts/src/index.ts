/**
 * Public API of `@smartestate/contracts`.
 *
 * Phase 0 ships the package boundary only (build, lint, typecheck, test wiring).
 * Phase 3 populates it with Zod schemas per domain (auth, listings, search, ...)
 * and the `z.infer` types consumed by both the NestJS DTO pipes and the React forms.
 */
export {};
