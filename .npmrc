# Refuse to install under a Node/pnpm version outside package.json "engines".
engine-strict=true
# Pin exact versions when adding dependencies; reproducibility matters for the thesis.
save-exact=true
# Keep the store strict: packages may only import what they declare.
auto-install-peers=true
strict-peer-dependencies=false
